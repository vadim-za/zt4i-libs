const ApiPreferences = @import("../../ApiPreferences.zig");

pub fn List(
    Payload: type,
    Layout: type,
    api_preferences: ApiPreferences,
) type {
    _ = Payload; // autofix
    _ = Layout; // autofix
    _ = api_preferences; // autofix
    return struct {};
}
